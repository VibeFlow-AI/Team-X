export interface Course {
    initials: string
    title: string
    author: string
    tags: string[]
    description: string
    features: string[]
    duration: string
    idealFor: string
  }
  
  export interface Feature {
    title: string
    img: string
  }
  